# @actiontree/web

The Actiontree Next.js app. It owns the UI, ENS profile API, `actions.json`, and Solana Action transaction endpoint.

Run commands from the monorepo root; the complete setup and architecture are documented in the root `README.md`.
